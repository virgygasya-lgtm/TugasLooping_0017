#include <iostream>
using namespace std;

// Variabel global
int n;
int choice;

// Fungsi cek bilangan prima
bool isPrime(int num) {
    if (num <= 1) return false;
    int i = 2;
    while (i <= num / 2) {
        if (num % i == 0) return false;
        i++;
    }
    return true;
}

// Kerangka fungsi cek bilangan Fibonacci
bool isFibonacci(int num) {
    // logika akan ditambahkan di commit berikutnya
    return false;
}

int main() {
    cout << "Program Tugas Looping dimulai..." << endl;
    return 0;
}